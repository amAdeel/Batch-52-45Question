let userName1=["Adeel(admin)","Naveed","Adnan","Dilawar","Ali"];
if (userName1.length===0) {
    console.log("We need to find some users!");   
}
// check whether a correct if condition excute or not
let userName2=[ ];
if (userName2.length===0) {
    console.log("We need to find some users!");   
}
